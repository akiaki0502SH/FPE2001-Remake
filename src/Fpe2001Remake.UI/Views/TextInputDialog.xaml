<Window x:Class="Fpe2001Remake.UI.Views.TextInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" Width="380" Height="170"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource WindowBrush}"
        FontFamily="{StaticResource UiFont}" FontSize="13"
        ShowInTaskbar="False" x:Name="Root">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding Label}" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,4" />
        <TextBox Grid.Row="1" x:Name="ValueBox" Height="30" VerticalContentAlignment="Center"
                 Padding="8,0" BorderBrush="{StaticResource LineBrush}" />
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="确定" Style="{StaticResource PrimaryButton}" Width="84" Click="Ok_Click" />
            <Button Content="取消" Style="{StaticResource GhostButton}" Width="84" Margin="8,0,0,0" Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>
